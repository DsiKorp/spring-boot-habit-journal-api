  1. Rebuild your project to download the updated dependency:
  ./mvnw clean install
  1. or if you use Maven directly:
  mvn clean install
  2. Restart your application - the Swagger UI should now load correctly at:
  http://localhost:8080/habit-journal/api/swagger-ui.html
  2. and the OpenAPI JSON at:
  http://localhost:8080/habit-journal/api/v3/api-docs

mvn package -DskipTests
java -jar target/habit-journal-api-0.0.1-SNAPSHOT.jar > server.log 2>&1 &
sudo lsof -i :8080
sudo kill -9 370916
